Jenkinsfile (Declarative Pipeline)
pipeline {
    agent any
    tools {
        maven "Apache Maven 3.3.9"
        java "jdk1.8.0_701"
    }

    stages {
        stage('Compile') {
            steps {
                echo 'Building..'
                sh 'mvn compile'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
                sh 'mvn test'
            }
        }
    }
}
