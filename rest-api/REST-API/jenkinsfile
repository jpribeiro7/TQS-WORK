pipeline {
    agent any
    tools {
        maven "Apache Maven 3.3.9"
    }

    stages {
        stage('Compile') {
            steps {
                echo 'Building..'
                sh 'cd REST-API'
                sh 'mvn compile'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
                sh 'cd REST-API'
                sh 'mvn test'
            }
        }
    }
}
